# FusedConv2d @ 5102 —— 内置算子的单算子验证

前提:算子已经作为 **CANN 内置算子**编进包并安装(不是自定义 vendor 包),手上有一颗
真的 5102。

```bash
source <CANN安装路径>/set_env.sh
cd <本目录>
./build_and_run_aclop.sh FusedConv2d 0     # 0 是 device id
```

一条命令跑完 0~5 步。下面解释每一步在验什么、以及为什么这么验。

> **这套东西只走 `aclopExecuteV2`,不走 aclnn。** 原本还有一份 aclnn 版的验证程序
> (`test_aclnn_fused_conv2d.cpp` / `build_and_run.sh` / `diagnose_aclnn.sh`),已经删掉 ——
> 这个算子没有生成公开的 aclnn 接口,下一节解释为什么这很正常、以及为什么 aclop
> 才是这类算子的常规验证方式。

---

## 0'. 为什么不走 aclnn:头文件缺了不是问题

`aclnnop/` 下没有 `aclnn_fused_conv2*.h`,这不是故障。

### aclnn 接口不是每个算子都有

**不是必有,也不是自动就有。** 它由 `op_host/CMakeLists.txt` 里的 `ACLNNTYPE` 决定:

| 取值 | 行为 | ops-nn 里的数量 |
| :--- | :--- | ---: |
| `aclnn` | opbuild 从 OpDef **自动生成** `aclnn_<op>.h/.cpp` | 195 |
| `aclnn_exclude` | **不生成**。要么自己在 `op_api/` 手写,要么干脆没有公开接口 | **523** |
| `aclnn_inner` | 生成内部接口 | 5 |
| `no_need_aclnn` | aicpu 算子 | 4 |

用 `aclnn_exclude` 的是**大多数**。最直接的旁证:CANN 9.2.0 的 `include/aclnnop/` 下有
956 个算子头,而 `Conv2DV2`、`Conv3DV2`、`QuantConv2D` **一个都不在里面**——它们都通过
`convolution_forward` 的 `aclnnConvolution` 对外。conv 目录下 14 个算子里 13 个是
`aclnn_exclude`。

所以 `aclnnop/` 下没有 `aclnn_fused_conv2*.h`,只说明**这个包没给它生成公开 aclnn 接口**,
不说明算子不能用。真正决定能不能跑的是:算子在不在算子信息库里、kernel 二进制编出来没有。

### 所以走 aclop

`aclopExecuteV2` 按**算子类型字符串**调用,不需要任何生成的头文件。这也是验
`Conv2DV2` 这类算子的常规做法:

```bash
g++ -std=c++17 -O2 test_aclop_fused_conv2d.cpp -o test_aclop_fused_conv2d \
    -I. -I$ASCEND_HOME_PATH/include \
    -L$ASCEND_HOME_PATH/lib64 -lascendcl -Wl,-rpath,$ASCEND_HOME_PATH/lib64
./test_aclop_fused_conv2d FusedConv2d 0
```

`build_and_run_aclop.sh` 就是把上面这几步(含 golden 自检)串起来。

**如果算子信息库里也找不到它** —— 那就不是接口问题,是这个算子压根没编进包。
先回去看构建日志里有没有它。

> 真的想要公开的 aclnn 接口的话:把 `op_host/CMakeLists.txt` 改成 `ACLNNTYPE aclnn`
> 再重新构建,生成物落在构建目录的 `autogen/` 下。注意这时候 **OpType 的大小写会
> 决定文件名**,见下一节。本仓库不再维护 aclnn 那条路。

---

## 0. 先确认算子名的大小写

`FusedConv2d`(小写 d)和 `FusedConv2D`(大写 D)会生成**两个不同的文件名和接口名**:

| OpType | 生成的头 | 接口 |
| :--- | :--- | :--- |
| `FusedConv2d` | `aclnn_fused_conv2d.h` | `aclnnFusedConv2d` |
| `FusedConv2D` | `aclnn_fused_conv2_d.h` | `aclnnFusedConv2D` |

opbuild 的 CamelCase→snake 转换在**每个大写字母前**插下划线,`FusedConv2D` 的 `D` 被
当成独立单词,于是 `fused_conv2_d`。脚本会自己探测,你也可以手查:

```bash
ls $ASCEND_HOME_PATH/include/aclnnop/aclnn_fused_conv2*.h
```

---

## 1~2. 确认跑的确实是你的算子

**这是内置算子路线和自定义 vendor 路线最大的区别。** 自定义包可以用"摘掉
`ASCEND_CUSTOM_OPP_PATH` 就该失败"来反证;内置算子摘不掉,得换别的办法。三条一起看:

**(a) 符号在不在内置库里**

```bash
for so in $ASCEND_HOME_PATH/lib64/libopapi*.so; do
  nm -D --defined-only "$so" | grep -i aclnnFusedConv2 && echo "  ^ in $so"
done
```

有输出才说明 aclnn 接口真的编进包了。没有的话不用往下走——`op_host/CMakeLists.txt`
八成不是 `ACLNNTYPE aclnn`。

**(b) 有没有自定义 vendor 把它盖住**

nnopbase 的查找顺序是

```
ASCEND_CUSTOM_OPP_PATH...  ->  $ASCEND_OPP_PATH/vendors...  ->  内置算子
```

**先命中先赢**。如果你以前装过带 FusedConv2d 的自定义包,它会**静默盖过**你新编进
内置的那个,你验的就是旧实现。验内置算子前:

```bash
unset ASCEND_CUSTOM_OPP_PATH
cat $ASCEND_OPP_PATH/vendors/config.ini      # 看 load_priority 里有哪些 vendor
find $ASCEND_OPP_PATH/vendors -iname '*fused_conv2d*' 2>/dev/null | head
```

最后一条有输出,就说明有个 vendor 包在抢。

**(c) 让 tiling 自证在场**

这个算子的 host tiling 只接受**一种 shape**,别的一律 `GRAPH_FAILED`。所以传一个错
shape 进去,**必须**在第一段接口就返回非 0:

```bash
# 把 x 的行数改成 129024(fp16 时代的形状,元素总数相同),应当被拒
```

如果错 shape 也能跑过去,说明服务这个 op 的不是你的 tiling。这条比看日志靠谱,
因为它验的是**行为**而不是元数据。

---

## 3. golden 自检 —— 先别碰设备

比对用的 golden 是 kernel UT 那一份原件(`fused_conv2d_int8_golden.h`),所以真机的
判据行可以和 CPU 仿真那条**逐字对比**。

跑之前先自检,确认 golden 本身可信:

```bash
g++ -std=c++17 -O2 golden_selfcheck.cpp -o golden_selfcheck && ./golden_selfcheck
```

> 别写成 `g++ -x c++ fused_conv2d_int8_golden.h`。那依赖编译器把 `.h` 当源文件处理,
> gcc 11 和 gcc 13 的行为不一致,不一致时报的是一句和 golden 毫无关系的
> ``undefined reference to `main'``。`golden_selfcheck.cpp` 就是为此存在的三行入口。

期望(实测):

```
layout round-trip:   x OK   w1 OK
conv1 acc  int32   min      -62084   max       57590   (int32 headroom 34590x)
scale1             min 0.00161075592  max 0.00228691101   (float19-exact, odd significand)
mid   int8         min  -100   max   100   nonzero 2030207 / 2064384   saturated 0   ties 0
conv2 acc  int32   min      -49325   max       53522
y int8             min  -100   max   100   saturated 0   ties 2
SELF-CHECK: PASS
```

三件事值得注意:

- **layout round-trip OK** 最重要。NC1HWC0 和权重 K 序两个变换要是写反了,后面
  再怎么比数值都发现不了。
- **ties 2 + 1**。存在恰好 x.5 的样本,意味着**舍入模式在这组数据上是可观测的**。
  没有 tie 的测试向量证明不了 round-half-to-even。
- **saturated 0**。全饱和的 golden 会让比对退化成"比 127 等不等于 127"。

---

## 4~5. 编译并跑

链的是内置库:

```bash
g++ -std=c++17 -O2 test_aclop_fused_conv2d.cpp -o test_aclop_fused_conv2d \
    -I. -I$ASCEND_HOME_PATH/include \
    -L$ASCEND_HOME_PATH/lib64 -lascendcl -Wl,-rpath,$ASCEND_HOME_PATH/lib64
./test_aclop_fused_conv2d FusedConv2d 0
```

(只链 `libascendcl`。aclnn 那条路要多链 `-lnnopbase -lopapi` 并且要 `aclnnop/` 下
的头,那份程序已经删掉了。)

期望输出的最后两行(`out_elems` / `golden_nonzero` 是 golden 算出来的,已知;
**`nonzero` 和 `mismatches` 是设备给的,新 shape 还没上过板,下面是判据不是实测**):

```
[fused-conv2d-5102-device] out_elems=774144 nonzero=774109 golden_nonzero=774109 unwritten=0 mismatches=0
[PASS] 774144 个输出全部写过，且与 CPU golden 逐位相等
```

对得上的参照是 CPU 仿真那条(实测):

```
out_elems=774144 nonzero=774109 golden_nonzero=774109 unwritten=0 mismatches=0
```

---

## 判据:三个数,少一个都不行

| 数 | 挡住的失败模式 |
| :--- | :--- |
| `unwritten == 0` | **kernel 什么都没写**。输出缓冲预先用哨兵 `0x7F7F` 铺满(y 是 fp16,这是个 NaN 位模式,golden 里不可能出现;程序启动时会断言这一点),剩下哨兵就说明没写过 |
| `nonzero == golden_nonzero` | **kernel 只写了一部分**。5102 上 KernelMode 选错会出现"后面几个 chunk 从来没被写过"——前一半完全正确,后一半空白 |
| `mismatches == 0` | 数值错 |

**逐位相等,不设容差**,而且这是有依据的而非侥幸:两次累加都在 int32 上且不可能溢出
(K ≤ 576,576×127×127 = 9.29e6 ≪ 2³¹),全算子唯一的舍入是两次 Fixpipe 转换
(conv1→mid 走 VREQ8 重量化,conv2→y 走 VDEQF16 反量化),golden 把这两次都建模到位了。
设容差就等于放弃这个信息。

**只看返回码 0 是不够的。** 这个算子有两种失败会伪装成成功,上面前两条就是专门堵它们的。

### 精度:相对误差达标比例

`run_fused_conv2d.py` 另外报一个逐元素的相对误差达标比例:

```
[精度] 相对误差 <= 0.001 的比例: 774144 / 774144 = 100.000000%   (下限 100.000000%)
       最大相对误差 0
       golden 为 0 的 35 个元素按绝对误差判
       相对误差分布:
         =0                774144  (100.000000%)
```

定义:golden 非 0 时 `|got-want| / |want|`;golden 为 0 时退化成绝对误差 `|got-want|`。

三个旋钮(环境变量):

| 变量 | 默认 | 作用 |
| :--- | :--- | :--- |
| `REL_TOL` | `1e-3` | 相对误差判据 |
| `RATIO_MIN` | `1.0` | 达标比例的下限,低于它判 FAIL |
| `REPEAT` | `1` | 计时的下发次数。想避开冷启动就调大,比如 10 |
| `WARMUP` | `0` | 正式计时前先空跑多少次(不计入统计)。总次数 = `WARMUP + REPEAT` |

> **输出改成 fp16 之后,这两条判据不再等价,而且宽严关系反了过来。**
>
> int8 时代:`|golden| <= 100`、最小非零差值是 1,对应相对误差 `>= 1e-2`,所以 1e-3
> 的判据严于逐位相等,达标比例只会是 100% 或恰好等于逐位相等的比例。
>
> fp16 时代:1 个 ULP 的相对误差最大只有 `2^-10 ≈ 9.77e-4`(尾数 10 位),**小于
> 1e-3**。差 1 个 ULP 的结果照样能通过 1e-3 的判据。所以现在是
> **逐位相等 严于 1e-3**。
>
> **以 `mismatches == 0` 为准。** 达标比例 100% 但 `mismatches != 0` 时脚本报
> `[PASS*]`,那不是通过。真正的信息量在**分布**那几行,以及 `位模式差 ±1` 的计数:
> 全是 1 ULP 说明舍入模型对不上,差得离谱说明布局或 scale 错了。

---

## 出错了怎么定位

程序会自己给出方向。核心的一条经验:**看错的幅度**。

| 现象 | 结论 |
| :--- | :--- |
| 位模式全部差 ±1(1 ULP) | 舍入模型不一致。y 走 VDEQF16:`acc` 精确转 fp32(`\|acc\| < 2²⁴`),乘 float19 的 scale,再舍到 fp16。用 double 算这个乘积会在边界附近分歧 |
| mid 错但 y 的形态还合理 | conv1 那一跳走的是 VREQ8:`acc*scale` **先舍到 fp32**,再 round-half-to-even 取整,offset 加在**取整之后**,最后才有符号饱和 |
| 差得离谱 / 大面积不对 | 布局或 scale 表。先看 golden 自检的 layout round-trip,再看 `PackScaleEntry` |
| mid 全是 0..255 的正数,没有负数 | **scale1 表的 bit[46] 忘了置 1**。这是最容易犯的错:算子照样跑完,给出一串看起来完全合理的无符号字节。注意这条**只对 scale1**:scale2 走 VDEQF16,只用 `[31:0]`,没有 offset 也没有饱和位,高 32 位必须是 0 |
| 输出是 `16807 / 282475249 / 1622650073 …` | 这是 minstd_rand 序列,说明那块内存**从来没被写过**,不是算错 |
| mid 大量饱和到 ±127 | scale1 偏大。golden 的 `ChooseScale` 会把 scale 对齐到 float19 可精确表示的值,自己造 scale 时也必须这么做,否则主机和器件对"scale 是多少"的理解就不一样 |
| 第一段接口返回 161001 | 算子没找到。回到第 1、2 步 |
| 前一半对、后一半全 0 | KernelMode / blockDim。这个算子是 `KERNEL_TYPE_AIC_ONLY`,5102 是 MIX_AIC_1_1,blockDim 必须是 **8**(HB=18,144/18 = 8 个 chunk,每核 1 个) |

### 还没跑到比对就挂了(编译/加载类)

| 现象 | 原因 |
| :--- | :--- |
| `cannot execute binary file: Exec format error` | 可执行文件的 CPU 架构和当前机器对不上——**几乎都是在 x86 开发机上编好、把二进制拷到 aarch64 板子上跑**。bash 这句话的意思只是 `execve` 返回了 ENOEXEC,和 CANN、和算子都没关系。用 `file` / `readelf -h` 对一下 Machine 和 `uname -m`。少数情况是文件被截断(磁盘满、传输中断),`ls -l` 看大小、`head -c4 \| xxd` 应当是 `7f45 4c46` |
| `undefined reference to drvHdcSessionConnect / halHdcRecv` | 缺驱动库 `/usr/local/Ascend/driver/lib64`。没插卡的机器上装不了,只能到有卡的机器上编 |
| `undefined reference to \`main'` | golden 头被截断了,不是 golden 算错(`grep -c "^int main()"` 应为 1) |
| `ld.lld: error: unknown emulation: aicorelinux` | PATH 上的 `ld.lld` 不是毕昇那份。一台机器上常有好几个 `ld.lld`(还多半是符号链接),只有 `bisheng_compiler` / `ccec_compiler` 下那个认识 `aicorelinux`,CANN 自己 `tools/llvm/` 下那个也不认。别按目录名猜,逐个探一遍,见下文 |
| 跑到某一行之后什么都不打印 | 老版脚本 `set -e` 的锅,新版已去掉 |

**跨机器时拷源码,不要拷可执行文件。** 这套东西只有 4 个文件、几秒钟就编完,在哪台机器上跑就在哪台机器上编。真的必须交叉编译时:

```bash
CXX=aarch64-linux-gnu-g++ CANN_ARCH=aarch64 ./build_and_run_aclop.sh
```

前提是这台机器上装了 aarch64 的 CANN(`$ASCEND_HOME_PATH/aarch64-linux/{include,lib64}`)和交叉工具链;脚本会检测出产物架构和本机不一致,编完就停,不会去碰设备。

---

## 目标机上没装带 FusedConv2d 的算子包

**先把一件事说清楚:编译验证程序不会把算子带过去。**

`aclopExecuteV2` 是**运行时按算子类型字符串分发**的。验证程序里只 include 了
`acl/acl.h` 和 `acl/acl_op.h`,只链了 `-lascendcl`,传进去的是字符串 `"FusedConv2d"`
—— 整个可执行文件里**一行算子代码都没有**。算子的实现(kernel 二进制 + 算子信息库
+ tiling)在执行时才从**运行那台机器**的 `$ASCEND_OPP_PATH` 里查。

所以"在装了算子包的 x86 上编译好、拷到 aarch64 上执行"解决不了这件事:程序照样能
起来,`aclopExecuteV2` 照样返回 **161001(算子没找到)**。aclnn 那条路更早就挂 ——
`aclnnFusedConv2d` 是 `libopapi.so` 里的符号,设备上那份没有,连加载都过不去。

两条真正可行的路:

### 甲. 把 aarch64 的算子包装到设备上（最干净）

前提是你手上有**给 aarch64 编的**那份包。x86 的包装不上去,也帮不上忙。
装完之后前面所有路线都直接可用。

### 乙. 单算子离线模型 .om（真正的"在 A 编、拿到 B 跑"）

`atc --singleop` 在有算子包的机器上把这一个算子**离线编译**成 `.om`,里面是给 5102
的 **device 二进制**。device 二进制和 host 的 CPU 架构无关,所以 x86 上编出来的 .om
拿到 aarch64 上照样能加载。目标机只要有基础的 CANN 运行时,**不需要装算子包**。

在 x86(有算子包的那台)上:

```bash
./build_om.sh
```

不传 `SOC` 时它只做诊断:确认这台机器的包里确实有 FusedConv2d、列出哪些 soc 编进了
它的 kernel、列出这个 CANN 支持的 `soc_version` 清单。从里面挑出 5102 对应的那个:

```bash
SOC=<soc_version> ./build_om.sh
```

#### soc_version 到底填什么

**权威来源只有一个:你这台机器的 `platform_config`。** 每个 `.ini` 的**文件名**就是一个
合法的 `soc_version`:

```bash
ls $ASCEND_HOME_PATH/compiler/data/platform_config/ | sed 's/\.ini$//'
```

`build_om.sh` 第 3 步会自动列这个清单,并把内容里带 `510` / `mc62` 字样的那几项单独
拎出来、连同 ini 里的短名和 aicore 架构一起打印 —— 5102 对应的那项基本就是它。

**别把 `soc_version` 和算子编译时的短名搞混。** 编 kernel 时用的 `--soc=mc62` 是**短名**,
ATC 要的是**完整的产品 soc_version**,两者不是一个东西,填短名 ATC 会拒。ini 里通常
两个都有(`short_soc_version=mc62` / `SocVersion=MC62xxxxxx`),对着看一眼就清楚了。

各家族 `soc_version` 长什么样,给个印象(**具体哪些存在取决于你的 CANN 版本,以上面的
清单为准**):

| 家族 | 形式 |
| :--- | :--- |
| 训练 910 | `Ascend910A` `Ascend910B` `Ascend910ProA` `Ascend910PremiumA` |
| 910B 系列 | `Ascend910B1` `Ascend910B2` `Ascend910B3` `Ascend910B4` |
| A3 | `Ascend910_9391` `Ascend910_9392` `Ascend910_9381` `Ascend910_9382` |
| 推理 310 | `Ascend310` `Ascend310P1` `Ascend310P3` |
| 边缘 310B | `Ascend310B1` ~ `Ascend310B4` |
| 车载 / 嵌入式 | `Ascend610Lite` `BS9SX1A` `BS9SX2A` `MC61AM21A` `AS31XM1X` |
| **5102 / MC62** | `MC62` 开头的那一项 —— 形式类似 `MC62CM12AA`,**以清单为准** |

注意最后一行:`MC62CM12AA` 是按前面上下文推的,不是我在你的机器上确认过的。清单里
`MC62` 开头只会有一项,照抄它就行。

如果 `platform_config` 目录不在标准位置,还有个笨办法:故意传一个不存在的
`soc_version`,ATC 的报错里通常会带上它认识的列表。

#### `unknown emulation: aicorelinux`

编 AI Core kernel 的最后一步是 `ld.lld -m aicorelinux`。**`aicorelinux` 这个 emulation
只有毕昇编译器带的那份 lld 认识。** 系统的 `/usr/bin/ld.lld`、conda 里 llvm 带的、
乃至 **CANN 自己 `tools/llvm/` 下那份通用 lld**,都不认。**不是算子的问题,是 PATH 顺序。**

坑在于一台机器上往往有好几个 `ld.lld`,而且**多数是符号链接**,光看目录名认不出来。
实测某台 CANN 9.2.0 上有三个:

| 路径 | 结果 |
| :--- | :--- |
| `tools/bisheng_compiler/bin/ld.lld` | **认识** |
| `tools/llvm/bin/ld.lld` | 不认识 |
| `<arch>-linux/bin/ld.lld` | 不认识(是前一个的符号链接) |

`<arch>-linux/bin` 通常排在 PATH 前面,于是撞上的正是不认识的那个。

**别按目录名猜,直接探。** 喂一个空输入让它走到 emulation 解析那一步,看它抱怨的是不是
`aicorelinux`:

```bash
for f in $(find $ASCEND_HOME_PATH -maxdepth 7 -name ld.lld 2>/dev/null); do "$f" -m aicorelinux /dev/null -o /tmp/.p$$ 2>&1 | grep -qi "unknown emulation" && echo "不认识  $f" || echo "认识    $f"; done; rm -f /tmp/.p$$
```

(注意 `find` 不要加 `-type f` —— 会把符号链接全漏掉。)

然后把**认识的那个**的目录提到最前:

```bash
export PATH=$ASCEND_HOME_PATH/tools/bisheng_compiler/bin:$PATH
```

**这一句要在 `source set_env.sh` 之后执行。** set_env.sh 会把编译器目录加进 PATH,
之后再 `conda activate`、`module load` 会重新往前插路径把它挤下去 —— 这是最常见的
成因,也是为什么"昨天还好好的"。

`build_om.sh` 第 0.5 步把这套流程做进去了:枚举 `ASCEND_HOME_PATH` 下和 PATH 上所有的
`ld.lld`,逐个探,打印"认识 / 不认识 / 跑不起来(缺依赖库)",符号链接会标出指向谁,
然后**在本次运行内自动把对的那个提到最前**并告诉你怎么永久生效。一个都不认识时直接停,
那说明毕昇编译器组件没装。

产物在 `om_out/`。连同 case 文件一起拷过去:

```bash
scp -r om_out fused_conv2d_case.bin run_fused_conv2d.py <5102机器>:~/a5102_st/
```

在 5102 机器上,第 4 个参数是 `.om`:

```bash
cd ~/a5102_st && source <CANN安装路径>/set_env.sh && python3 run_fused_conv2d.py fused_conv2d_case.bin FusedConv2d 0 om_out/0_FusedConv2d_1.om
```

**这个参数既可以是单个 `.om` 文件,也可以是装着 `.om` 的目录:**

| 传的是 | 走的接口 | 效果 |
| :--- | :--- | :--- |
| `om_out/xxx.om` | `aclopLoad` | 只注册这一个 |
| `om_out` | `aclopSetModelDir` | 目录下**所有** `.om` 都加载 |

`aclopSetModelDir` 是 ACL 的接口,它只吃目录 —— 所以给单个文件时脚本改走 `aclopLoad`
(把文件读进内存再注册)。目录里有多个 `.om` 时,指名到文件更干净:不会把无关的模型
也拉进来参与匹配。传目录时脚本会把数出来的 `.om` 列给你,超过一个还会提醒。

老版本的 CANN 没有 `aclopLoad` 的话,会退回"取所在目录 + `aclopSetModelDir`",并明确
告诉你这会加载整个目录。

`run_op.py` 的 `om_dir` 字段同样两者都接受。

**这条路要留意的两点:**

- `.om` 是按**固定 shape** 编的,tiling 在编译期就定死了。所以 `fused_conv2d_singleop.json`
  里的 shape/dtype 必须和 case 文件完全一致(新 shape 下是 7 个输入 + 1 个输出,
  1032192/18432/256/512/55296/384/768 -> 1548288 字节,逐个对得上)。
- `.om` 的 CANN 版本要和设备上的运行时兼容。差太多会在加载时报错,不是执行时。

（甲我没法替你验;乙的 shape 一致性和脚本的各处检查我验过,但这里没有装了算子包的
CANN,`atc` 那一步没法实跑。）

---

## 目标机器上没有编译器怎么办

这是个很常见的情况:开发机是 x86 且装着全套工具链,插着 5102 的那台是 aarch64 且
只装了运行时。**把 x86 上编出来的二进制拷过去是不行的**,一跑就是
`cannot execute binary file: Exec format error`。

三条路,按推荐程度排:

| 路线 | 目标机需要 | 风险 |
| :--- | :--- | :--- |
| **A. 预生成 case + Python 跑**(推荐) | 只要 `python3` | 无 |
| B. 交叉编译 | 什么都不要 | glibc 版本错配 |
| C. 在目标机上装 g++ | g++ + CANN 头 | 无,但常常没权限 |

### A. 预生成 case + Python 跑

把工作切成两半:**算 golden**(纯 host、和架构无关)放在能编译的机器上做,
**调设备**(纯 API 调用)放在目标机上用 ctypes 做,中间靠一个 `.bin` 传递。
目标机上一行 C++ 都不用编。

在 x86 开发机上:

```bash
./build_case.sh
```

它跑 golden 自检、编 `gen_case`、产出 `fused_conv2d_case.bin`(新 shape 下 2,656,696
字节,7 个输入 + golden 输出),最后把 scp 命令打给你。

然后:

```bash
scp fused_conv2d_case.bin run_fused_conv2d.py <5102机器>:~/a5102_st/
```

在 5102 机器上:

```bash
cd ~/a5102_st && source <CANN安装路径>/set_env.sh && python3 run_fused_conv2d.py fused_conv2d_case.bin FusedConv2d 0
```

`run_fused_conv2d.py` 用 ctypes 直接绑 `libascendcl.so` 里的 `aclopExecuteV2`,
**不需要 numpy、不需要 pyACL、不需要 aclnn 头**。判据和输出行与 C++ 版逐字相同。

> **golden 跨架构是安全的,这是验过的而不是假设的。** golden 里唯一的浮点乘是
> `RequantToInt8` 里的 `(double)((float)acc * scale)`,后面跟的是取整不是加法,
> 所以不存在可被 `-ffp-contract` 融成 `fmadd` 的 `a*b+c`(脚本还是显式加了
> `-ffp-contract=off` 把这件事钉死);数据全用 `int8_t` 而不是裸 `char`
> (ARM 上 `char` 默认无符号,这是另一个经典跨架构坑)。实测:在 aarch64 + clang 上
> 生成的 case,`nonzero=508099 ties=3 sat=0`、`acc1_range=[-77278,83979]`,
> 与 x86 + gcc 的记录逐字一致。

传完两边各跑一次 `md5sum` 对一下。`.bin` 传坏了脚本会报"文件被截断",
但传成**另一个完整文件**它看不出来。

### B. 交叉编译

```bash
scp <5102机>:$ASCEND_HOME_PATH/lib64/libascendcl.so ~/cann-aarch64/lib64/
AARCH64_LIB=~/cann-aarch64/lib64 ./build_cross.sh
```

只需要目标机的 `libascendcl.so` 一个文件——脚本加了 `-Wl,--allow-shlib-undefined`,
链接器就不再要求解析它 DT_NEEDED 下去的那一长串(最终连到驱动的 `libascend_hal.so`),
那些在目标机上全都有。acl 头文件是架构无关的,用本机 x86 CANN 里的那份即可。

**这条路唯一的坑是 glibc。** 交叉工具链自带的 glibc 比目标机新的话,产物会要求
`GLIBC_2.34` 之类的符号版本,拿过去一跑就是 `version ``GLIBC_2.34`` not found`。
脚本第 3 步会把产物要求的最高版本打出来,拿它和目标机的 `ldd --version` 比一下。
比不过就退回 A。

（A 我在本地端到端跑通了;B 的脚本逻辑和各处报错检查过,但我这里没有 aarch64
交叉工具链,没法替你实跑,第一次用请留意它每一步的输出。）

---

## 附:算子的输入约定

ABI 顺序钉死,scale 插在各自的 bias 之后:

| 参数 | 形状 | dtype | 说明 |
| :--- | :--- | :--- | :--- |
| x | `[32256, 32]` | int8 | NC1HWC0 拍平,C0=32、**C1=1**、Hi=288、Wi=112 |
| filter1 | `[64, 288]` | int8 | K 序 `(c1, kh, kw, c0)`,K1 = 1·9·32 |
| bias1 | `[64]` | int32 | |
| scale1 | `[64]` | **uint64** | **VREQ8** 表,**调用方打包** |
| filter2 | `[96, 576]` | int8 | 同 K 序,K2 = 2·9·32。**注意 K1 ≠ K2,两个 filter 不可互换** |
| bias2 | `[96]` | int32 | |
| scale2 | `[96]` | **uint64** | **VDEQF16** 表,编码和 scale1 不同,见下 |
| y | `[8064, 96]` | **float16** | conv1 3×3 s1 p1 → mid[1,64,288,112] int8 → conv2 3×3 s2 p1 |

scale 表项的位域:

| 位域 | 含义 |
| :--- | :--- |
| `[31:13]` | scale 的 fp32 位模式,低 13 位尾数丢弃(等效值 = `bitcast<float>(u32 & 0xFFFFE000)`) |
| `[45:37]` | 有符号 9 位补码 offset,加在**取整之后**的整数上 |
| `[46]` | **1 = 有符号饱和到 [-128,127]**;0 = 无符号到 [0,255] |

上表是 **scale1**(VREQ8)。**scale2** 走 VDEQF16,只用 `[31:0]` 的 fp32 位模式,
没有 offset 也没有饱和位,高 32 位必须是 0 —— 打包函数是分开的两个
(`PackScaleEntry` / `PackDeqF16Entry`),不要合并。

`PackScaleEntry()` 就是干这个的,直接抄。注意 ops-nn 其它地方出现的
`0x3F80000000000000` 写法(把 fp32 放在高半部)对这条通路是**错的**,实测得 0。

---

## 性能:msprof

功能过了再谈性能。一条命令:

```bash
./run_prof.sh fused_conv2d_case.bin FusedConv2d 0 om_out
```

(没走离线模型就把最后那个参数去掉。)

它做四件事:找到 `msprof` -> 带采集开关跑一次 -> 尝试 `--export=on` -> 解析结果。
采集期间 `run_fused_conv2d.py` 会先做完整的正确性 + 精度比对,再额外下发 `REPEAT` 次
(默认 20),所以**一次采集同时拿到功能、精度、性能**。

| 变量 | 默认 | 作用 |
| :--- | :--- | :--- |
| `REPEAT` | `20` | 采集期间下发多少次(计时的) |
| `WARMUP` | `0` | 采集期间的预热次数。**采性能一般不设** —— msprof 按每次任务分别统计,冷启动那次在 min/p50/max 里一眼能认出来,没必要提前扔掉 |
| `OUT` | `./prof_out` | msprof 输出目录 |
| `MSPROF_ARGS` | `--ai-core=on --task-time=on --aic-metrics=PipeUtilization` | 采集开关 |

输出长这样:

```
  task_type=AIC_SQE      n=21    min=   12.612  p50=   12.776  mean=   13.157  max=   21.135  us
  kernel 合计              n=21    min=   12.612  p50=   12.776  mean=   13.157  max=   21.135  us 总设备时间 276.297 us
```

**看 min / p50,不要看 mean。** 第一次下发含 kernel 加载,通常显著偏大 —— 上面那组里
`max=21.1` 就是它,而 p50 是 12.8。

### 三个已经踩过的坑

- **开关名各版本不一样。** 带 `--aic-metrics` 被拒时脚本会自动用最小开关重试一次,
  不用你先查文档。要指定就 `MSPROF_ARGS='--ai-core=on' ./run_prof.sh ...`。
- **落盘位置各版本不一样。** `parse_prof.py` 按四个位置依次找:
  `mindstudio_profiler_output/op_summary_*.csv` -> `device_*/summary/op_summary_*.csv`
  -> `device_*/sqlite/ai_core_op_summary.db` 的 `task_time` 表 -> 任何带 `task_time` 表的
  `.db`。全都没有时它会把目录里实际有什么列出来,照着补路径就行。
- **芯片被占时 msprof 会静默挂住** —— 不报错、不超时、没有输出。所以采集套了
  `timeout 900`;机器上没有 `timeout` 命令时会降级并明确告诉你没有超时保护。

### 没有 msprof 的话

设备上装的只是 nnrt 运行时时不带 profiler。退而求其次:

```bash
REPEAT=50 python3 run_fused_conv2d.py fused_conv2d_case.bin FusedConv2d 0 om_out
```

会打印 host 侧墙钟的 `min/p50/max/first`。**那是含下发和同步开销的上界**,和 msprof
的设备侧时间不是一回事,别混着报。

---

## 只有 .om 和输入 bin,别的什么都没有

**能跑。`.om` 本身就带着它要什么输入 —— 不需要 json,也不需要知道 attr。**

```bash
python3 run_om.py your_model.om
```

不带 bin 参数时它只做一件事:加载模型,把接口打印出来。**先跑这个。**

```
==== 这个 om 的接口（从模型自己身上问出来的）====
  输入: 3 个
    [0] float16    NCHW          1x3x224x224        301056 字节  150528 元素
    [1] float16    NCHW          64x3x7x7            18816 字节  9408 元素
    [2] float32    ND            64                    256 字节  64 元素
  输出: 1 个
    [0] float16    NCHW          1x64x112x112      1605632 字节  802816 元素
```

对上号之后按这个顺序喂 bin:

```bash
python3 run_om.py your_model.om in0.bin in1.bin in2.bin --device=0
```

要比精度就加 `--golden0=y.bin`(第 N 个输出对应 `--goldenN=`)。不加就只跑不比,
但仍然会用哨兵回答"kernel 到底写没写"。

### 连跑多次,把冷启动摘出来

单跑一次拿到的数含 kernel 加载等一次性开销,不能当稳态性能用。

```bash
python3 run_om.py your_model.om in0.bin in1.bin in2.bin --repeat=10
```

```
==== 耗时（host 侧墙钟，含下发和同步；设备侧以 msprof 为准）====
  全部             n=10   min=   12.610  p50=   12.795  mean=   13.630  max=   21.135  stdev=   2.505  us
  第 1 次          21.135 us
  去掉第 1 次        n=9    min=   12.610  p50=   12.790  mean=   12.796  max=   13.020  stdev=   0.127  us
  冷启动开销≈         8.345 us  (第 1 次 21.135 - 稳态中位数 12.790)
```

第 1 次单独报,再给一份"去掉第 1 次"的统计,冷启动多贵一眼可见。`stdev` 顺带告诉你
这组数稳不稳 —— 上面稳态那组 0.127us 就是很稳,要是和均值同量级,说明机器上有别的负载,
数不能用。

不想看冷启动就让它被预热吃掉:

```bash
python3 run_om.py your_model.om in0.bin in1.bin in2.bin --warmup=3 --repeat=10
```

总下发次数 = `warmup + repeat`,只有后 `repeat` 次计入统计。判据看的是**最后一次**的
输出,所以连跑也顺带验了稳态下依然写对。

`run_op.py` 和 `run_fused_conv2d.py` 同样支持,用环境变量 `REPEAT` / `WARMUP`。

> 这仍然是 **host 侧墙钟**,含下发和同步开销,只能当上界。要设备侧的真实 kernel 时间
> 还是 `./run_prof.sh`。两者不要混着报。

### 和 run_op.py 的区别

| | `run_op.py` | `run_om.py` |
| :--- | :--- | :--- |
| 走的接口 | `aclopExecuteV2` | `aclmdlLoadFromFile` + `aclmdlExecute` |
| 要你提供 | shape / dtype / format / **全部 attr** | **什么都不用** |
| 适用 | 算子在本机算子信息库里,或有 .om 且知道描述 | **只有 .om** |

attr 在编 `.om` 的时候就固化进模型里了,所以走模型加载这条路根本不需要它们 ——
这也顺带绕开了"attr 对不上导致 161001"那个最难查的坑。

### 输入顺序错了会被抓住

每个 bin 的字节数会和模型报的逐个核对:

```
  [0] in0.bin                        301056 字节  OK
  [1] in1.bin                         18816 字节  OK
  [2] in2.bin                           512 字节  ** 应为 256，差 +256 **
```

顺序颠倒通常也表现为字节数对不上。三个输入字节数刚好相同的模型才需要你自己确认顺序。

### dims 读不出来不影响执行

`aclmdlGetInputDims` 要按 `aclmdlIODims` 结构体的偏移去解,那个结构体的常量各版本
可能不同。脚本解完会用"各维乘积 x 每元素字节数 == 模型报的字节数"验一遍,验不过就
显示 `dims<读不出>` —— **执行只需要字节数**,dims 纯粹是打印给人看的。

### 万一这个 .om 加载不了

`atc --singleop` 产出的单算子模型有可能只能走 `aclopSetModelDir` 那条路。真碰上了,
在装了 CANN 的机器上把 om 反解成 json:

```bash
atc --mode=1 --om=your_model.om --json=your_model.json
```

拿到 json 就能还原出 op 类型、各 tensor 的 shape/dtype/format 和 attr,再用
`run_op.py` 跑。这份 json 是图 dump,结构和编 `.om` 用的 singleop json 不一样,
`singleop2case.py` 不能直接吃 —— 发我我照着生成。

### 性能

```bash
APP_CMD="python3 run_om.py your_model.om in0.bin in1.bin in2.bin" OPTYPE=Conv2D ./run_prof.sh
```

---

## 换个算子跑:通用执行器

`run_fused_conv2d.py` 把 7 个输入、固定 shape 和 golden 全写死了,只服务这一个算子。
执行那半截(ctypes -> `aclopSetModelDir` -> `aclopExecuteV2`)其实和算子无关,抽成了
`run_op.py`:任意算子、任意个输入、裸 `.bin` 进出,**golden 可选**。

```bash
python3 run_op.py conv2d_case.json 0
```

`case.json` 描述一次执行:

```json
{
  "op": "Conv2D",
  "om_dir": "om_out",
  "inputs": [
    {"file": "x.bin",      "dtype": "float16", "shape": [1,3,224,224], "format": "NCHW"},
    {"file": "filter.bin", "dtype": "float16", "shape": [64,3,7,7],    "format": "NCHW"},
    {"file": "bias.bin",   "dtype": "float32", "shape": [64],          "format": "ND"}
  ],
  "outputs": [
    {"dtype": "float16", "shape": [1,64,112,112], "format": "NCHW", "file": "y_out.bin"}
  ],
  "attrs": [
    {"name": "strides",     "type": "list_int", "value": [1,1,2,2]},
    {"name": "pads",        "type": "list_int", "value": [3,3,3,3]},
    {"name": "dilations",   "type": "list_int", "value": [1,1,1,1]},
    {"name": "groups",      "type": "int",      "value": 1},
    {"name": "data_format", "type": "string",   "value": "NCHW"}
  ]
}
```

### 别手抄 —— 从编 .om 那份 json 生成

```bash
python3 singleop2case.py conv2d_singleop.json conv2d_case.json --om-dir=om_out
```

**走离线模型时,ACL 是拿"op 类型 + 每个 tensor 的 shape/dtype/format + 全部 attr"去匹配
`.om` 的。** 任何一项和编 `.om` 时不一致都匹配不上,而报出来的是**算子没找到(161001)** ——
极容易误判成算子没编进包。从编 `.om` 的那份 singleop json 直接生成,就没有手抄出错的
余地;生成完只要把 `inputs[].file` 改成你实际的 bin 名字。

### 没有 golden 也能验出点东西

输出缓冲预填哨兵 `0xA5`,所以即使不比精度也能回答"kernel 到底写没写":

```
==== 输出[0] ====
  元素 802816，字节 1605632
  还是哨兵(0xA5)的字节: 0 / 1605632   全 0 的字节: 12
```

哨兵字节 == 总字节 -> **kernel 一个字节都没写**,直接判 FAIL;超过 10% -> 提示可能只写了
一部分。给 `outputs[N]` 加一个 `"golden": "xxx.bin"` 才开精度比对(相对误差 + 分布直方图,
`REL_TOL` / `RATIO_MIN` 同前)。

### dtype / format

名字表见 `run_op.py` 顶部。**也可以直接写 ACL 的枚举整数**(比如 `"dtype": 27`),
万一名字表在你这个 CANN 版本上对不上,不至于卡住。

`attrs` 支持 `bool int float string dtype list_bool list_int list_float list_string
list_list_int`,类型名沿用 ATC singleop json 的写法。不认识的类型在**碰设备之前**就报错。

### 输入 bin 的尺寸是硬校验

`shape x dtype` 算出来的字节数和文件实际大小对不上就直接停,并告诉你差多少:

```
[X] inputs[0]: bad.bin 是 301000 字节，但 shape=[1, 3, 224, 224] dtype=float16 需要 301056 字节。
    shape 或 dtype 写错了 —— 这是最常见的一类错，先改这里。
    (差 -56 字节)
```

### 性能

```bash
APP_CMD="python3 run_op.py conv2d_case.json" OPTYPE=Conv2D ./run_prof.sh
```

`REPEAT` 同样有效。

---

## 文件清单

| 文件 | 作用 |
| :--- | :--- |
| `fused_conv2d_int8_golden.h` | CPU golden,和 kernel UT 用的是同一份。所有路线共用 |
| `golden_selfcheck.cpp` | golden 自检的入口(三行,只为避开 `-x c++ header.h` 的坑) |

**目标机能编译时:**

| 文件 | 作用 |
| :--- | :--- |
| `build_and_run_aclop.sh` | 0~5 步一条龙,只链 `libascendcl` |
| `test_aclop_fused_conv2d.cpp` | 真机验证程序,走 `aclopExecuteV2` |

**目标机不能编译时(路线 A / B):**

| 文件 | 跑在哪 | 作用 |
| :--- | :--- | :--- |
| `build_case.sh` | 开发机 | 自检 golden + 生成 case 文件,一条龙 |
| `gen_case.cpp` | 开发机 | 把 7 个输入 + golden 打成 `fused_conv2d_case.bin`。**不依赖 CANN** |
| `run_fused_conv2d.py` | 5102 机器 | ctypes 直调 `aclopExecuteV2`,**不需要编译器/numpy/pyACL**。功能 + 精度 + 粗测性能 |
| `run_prof.sh` | 5102 机器 | msprof 采集 + 解析,一条命令。`APP_CMD` 可换成别的算子 |
| `run_op.py` | 5102 机器 | **通用**单算子执行器:任意算子、裸 bin 进出、golden 可选。要你提供 shape/dtype/attr |
| `run_om.py` | 5102 机器 | **只有 .om 和输入 bin** 时用。接口从模型自己身上问,不需要任何描述文件 |
| `singleop2case.py` | 任意 | 把编 `.om` 的 singleop json 转成 `run_op.py` 的 case.json |
| `parse_prof.py` | 5102 机器 | 解析 msprof 落盘(csv / sqlite 四种布局都试) |
| `build_cross.sh` | 开发机 | 备选:交叉编译出 aarch64 二进制(有 glibc 风险) |

**目标机上没装算子包时(路线乙):**

| 文件 | 跑在哪 | 作用 |
| :--- | :--- | :--- |
| `build_om.sh` | 有算子包的机器 | 诊断 + `atc --singleop` 编出 `.om` |
| `fused_conv2d_singleop.json` | 同上 | 单算子的 shape/dtype 描述,与 case 文件逐字节对齐 |
| `run_fused_conv2d.py` | 5102 机器 | 第 4 个参数传 `.om` 目录即走离线模型 |
